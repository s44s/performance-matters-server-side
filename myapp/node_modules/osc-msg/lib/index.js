"use strict";

var decode = require("./decode");
var encode = require("./encode");

module.exports = {
  decode: decode,
  encode: encode,
  fromBuffer: decode,
  fromObject: encode,
  toBuffer: encode,
  toObject: decode
};